# Emporix Terraform Provider - Test Environment Variables
# Copy this file to .env.test and fill in your values
# DO NOT commit .env.test to version control!

# Enable acceptance tests (required)
export TF_ACC=1

# Emporix credentials (required)
export EMPORIX_TENANT="your-test-tenant"
export EMPORIX_CLIENT_ID="your-oauth-client-id"
export EMPORIX_CLIENT_SECRET="your-oauth-client-secret"

# Emporix API URL (optional, defaults to https://api.emporix.io)
export EMPORIX_API_URL="https://api.emporix.io"

# Usage:
# 1. Copy this file: cp .env.test.example .env.test
# 2. Fill in your test tenant credentials in .env.test
# 3. Load before running tests: source .env.test
# 4. Run tests: make testacc

# Required OAuth Scopes:
# - site.site_read
# - site.site_manage
# - payment.payment_manage
# - payment.payment_read
# - country.country_read
# - country.country_manage

# IMPORTANT:
# - Use a TEST tenant, never production!
# - Tests will create/modify/delete real resources
# - Add .env.test to your .gitignore
